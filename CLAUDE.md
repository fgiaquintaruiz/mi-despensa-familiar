@AGENTS.md

## Cross-Agent Synchronization
- **Mandatory Synchronization**: Any modification made to this file (`CLAUDE.md`) MUST be replicated to `GEMINI.md` to ensure both AI agents share the exact same project mandates and conventions. Always use `mem_save` to record these synchronization actions in Engram.
